// Preload script for security
// Currently empty but can be used for exposing safe APIs to renderer

window.addEventListener('DOMContentLoaded', () => {
  console.log('⚡ Relampo YAML Editor - Desktop App');
});
